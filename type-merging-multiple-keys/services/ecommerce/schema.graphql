type Product @key(selectionSet: "{ id upc }") {
  id: ID!
  upc: ID!
  retailPrice: Int
  unitsInStock: Int
}

input ProductKey {
  id: ID
  upc: ID
}

type Query {
  productsByKey(keys: [ProductKey!]!): [Product]! @merge
  _sdl: String!
}
