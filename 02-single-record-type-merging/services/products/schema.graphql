type Product {
  manufacturer: Manufacturer!
  msrp: Float!
  name: String!
  upc: ID!
}

type Manufacturer {
  id: ID!
  products: [Product]!
}

type Query {
  product(upc: ID!): Product
  _manufacturer(id: ID!): Manufacturer
}
