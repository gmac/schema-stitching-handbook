type Post {
  id: ID!
  message: String!
}

type Query {
  posts: [Post]!
  sdl: String!
}

type Mutation {
  createPost(message: String!, userId: ID): Post!
}

type Subscription {
  newPost: Post!
}
