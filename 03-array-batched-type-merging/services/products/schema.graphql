type Product {
  manufacturer: Manufacturer!
  msrp: Float!
  name: String!
  upc: ID!
}

type Manufacturer {
  id: ID!
  products: [Product]!
}

type Query {
  products(upcs: [ID!]!): [Product]!
  _manufacturers(ids: [ID!]!): [Manufacturer]!
}
